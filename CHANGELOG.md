# Changelog

本项目从首个 GitHub 公开版本开始遵循语义化版本。公开版本线从 `0.1.0` 起步；此前的 `0.9.x` 仅为本地开发构建编号，不属于公开发行历史。

## Unreleased

## 0.3.1 - 2026-08-26

- Workbench 项目行新增“新建项目会话”快捷按钮，点击后在对应 DSH Workspace 中创建或复用空白会话，不再打开最近一条历史会话。
- 项目概览、会话图谱和删除项目收进 DSH 原生“更多项目操作”菜单；菜单使用 body portal，避免被工作台展开容器或侧边栏滚动区域裁切。
- 项目行只保留“＋”与“…”两个操作入口，默认弱化、悬停或键盘聚焦时清晰显示，触屏设备保持可见。
- 客户与项目取消独立展开箭头，点击名称行直接展开或收起下级；当前会话所在层级自动展开，并允许手动收起。
- 客户行同步保留“＋”与“…”：“＋”新建客户目录会话，重命名与安全删除收进 DSH 原生菜单。
- 客户与项目使用开合文件夹图标表达目录状态；展开行为不再误打开最近历史，会话仍需点击具体会话行进入。

## 0.3.0 - 2026-08-25

- 侧边栏顶部新增跨普通项目与全部 Workbench 的“进行中”虚拟分区，不改变会话原有归属，也不复制会话数据。
- 直接消费 DSH 原生 `pendingInteraction`、`running` 与 `completed` 状态，按“等待处理、运行中、刚完成”排序；后台完成提醒在进入会话后由 DSH 自动清除。
- 每条活动会话显示所属普通项目或“工作台 / 目录”路径并支持一键进入；侧边栏搜索同时匹配会话标题和归属路径。
- 运行、等待与完成状态使用独立视觉标识，动态效果遵从系统“减少动态效果”设置。

## 0.2.1 - 2026-08-25

- 工作台目录改为高度与透明度协同的展开/收起过渡，避免内容区域通过 `display: none` 硬切换造成闪烁。
- 工作台目录首次挂载时立即读取数据，并在首次请求结束前抑制短暂的错误空状态。
- 展开时同步平滑调整工作台行的操作区留白，并尊重系统“减少动态效果”设置。

## 0.2.0 - 2026-08-25

- 每个 Workbench 项目新增“会话图谱”入口，在 DSH 主内容区按原生 Session 父子关系展示该项目当前未归档会话。
- 会话卡片支持拖动、画布平移/缩放、自动适配和本机布局记忆；项目目录或工作台根目录重定位后仍使用稳定项目身份恢复布局。
- 按需展开最近会话轮次，仅投影用户问题、回答摘要和工具步骤数量，不复制工具参数、工具结果或完整会话日志。
- 已完成轮次支持调用 DSH 原生 `sessions.fork({ sessionId, atSeq })` 精确分支；新分支继承 DSH 的项目 Workspace 归属并直接进入会话。
- 图谱内支持使用 DSH 原生归档能力隐藏会话；归档、外部列表变化和项目路径迁移会实时收敛，空态、读取失败和分支失败均有独立反馈。
- 图谱通过 DSH `shell.overlay` 插槽组合，不增加第二套侧边栏，不修改 DSH 官方源码，也不保存或调用额外模型。

## 0.1.3 - 2026-08-25

- 空的“最近会话”区域不再渲染，普通项目直接承接侧边栏空间。
- 无 DSH Workspace 归属的历史会话改称“未归类会话”，仅在确有内容时按数量显示，并默认折叠；搜索命中时自动展开。
- 未归类会话新增“移入项目”操作，通过 DSH 官方 Workspace API 持久更新会话归属，不移动或改写会话日志与项目文件。

## 0.1.2 - 2026-08-24

- 新增公共列表插槽 `sidebar.settings.trailing`，供独立插件在侧边栏“设置”右侧提供紧凑快捷操作。
- 展开侧边栏时快捷操作与“设置”同行；折叠时自动排列到“设置”上方，保持按钮可见且不依赖绝对定位。
- 插槽为空时不占空间；消费者缺失或未安装时，Workbench 原有布局和功能保持不变。

## 0.1.1 - 2026-08-24

- 修复 Workbench 底部插件插槽裁剪第三方悬浮窗的问题；常驻插件按自然高度排列，上方会话与工作台列表继续独立滚动。
- 兼容展开与折叠侧边栏中的 `sidebar.footer.action` 浮层，不针对特定插件写特殊选择器。

## 0.1.0 - 2026-08-24

- 提供 Codex 式单列侧边栏，统一普通会话、普通项目和多个 Workbench。
- 按“工作台 → 客户 → 项目 → 会话”组织内容，并保证会话只显示在一个归属层级。
- 支持工作台创建、设置、排序、移除和根目录重新关联。
- 支持客户与项目的创建、重命名、移动、归档和 `.trash` 安全删除。
- 支持项目文件分类、搜索、系统打开以及模型项目引用。
- 支持 DSH 模型、Agent preset、permission preset 和可选辅助能力策略。
- 项目 overlay 使用“工作台 + 客户 + 项目”作用域，防止不同客户同名项目互相覆盖。
- 搜索基于完整目录树计算会话所有权，避免会话在搜索期间漂移到错误层级。
- 模型删除项目工具要求显式确认；重复回收目标自动添加序号，避免覆盖。
- 提供 GitHub CI、自动 Release、SHA-256 校验以及公开安全和开发文档。
